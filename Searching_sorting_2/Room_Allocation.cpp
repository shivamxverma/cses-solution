#include <iostream>
#include <vector>
#include <map>
#include <algorithm>

using namespace std;

using ll = long long;


void solve() {
    ll n, k;
    cin >> n >> k;
    
    map<int,long long> freq;
    
    for (ll i = 0; i < n; i++) {
    }

    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    solve();
    return 0;
}